{
  "name": "leave-shift-manager",
  "version": "1.0.0",
  "description": "Complete MERN Leave and Shift Manager Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "chai": "^5.2.1",
    "chai-http": "^5.1.2",
    "concurrently": "^7.0.0",
    "mocha": "^11.7.1"
  },
  "author": "Leave and Shift Manager Team",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
